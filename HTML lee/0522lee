<!--변수선언 키워드
1.var ex) var x=1; (var 쓰는건 비추천)
2.let: 변수 수정가능
3.const:상수(수정불가능)
-->
<!-- 자바와 자바스크립트차이점
자바스크립트 : let x= []
자바 : int x[]={} -->
<!-- let x; = undefined(존재하는 값이 없다)-->
<!DOCTYPE html>
<html lang="kr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!--viewport : 그래픽화면 설정영역-->
    <title>Document</title></head
  ><!--head는 meta와 title를 자식으로 가지고 있다-->
  <body>
    <h1>Javascript page</h1>
    <div>
      <p>이것은 p태그 요소입니다.</p>
    </div>
    <script>
      var currentYear = 2024;
      var birthYear;
      var age;

      let x;
      console.log(x);
      x = 100;
      console.log(x);
      const y = 10;
      y = 100;

      // alert("자바스크립트 예제");
      // birthYear = prompt("태어난 연도를 입력하세요");
      // age = currentYear - birthYear + 1;
      // document.write(currentYear + "년 현재<br>");
      // document.write(birthYear + "년에 태어난 사람의 나이는" + age + "세입니다.");
    </script>
  </body>
</html>